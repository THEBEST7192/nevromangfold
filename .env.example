# Database configuration (used by backend/server.js or Render deployment)
DB_HOST=your_database_host
DB_PORT=3306
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=your_database_name
PORT=3001

# Frontend / CORS configuration
CLIENT_ORIGIN=http://localhost:5173
# When running Express backend locally the API lives on http://localhost:3001
VITE_API_BASE_URL=http://localhost:3001

# Cloudflare Access (optional - only needed when DB is behind Cloudflare Zero Trust)
CF_ACCESS_HOSTNAME=your-cloudflare-access-hostname
CF_ACCESS_CLIENT_ID=your_cloudflare_access_client_id
CF_ACCESS_CLIENT_SECRET=your_cloudflare_access_client_secret
DB_TUNNEL_PORT=53306
